# 변경 로그

## 2026-08-17

### Vercel 배포 안정화

- 모든 런타임 의존성 버전을 고정하고 OpenAI, Markdown, bleach를 추가했습니다.
- 정적 파일과 템플릿 경로를 `main.py` 기준 절대 경로로 변경했습니다.
- Starlette 최신 `TemplateResponse(request=, name=, context=)` 호출 형식을 적용했습니다.
- DB 연결 또는 조회 실패 시 회사 화면이 500 대신 안전한 안내 화면을 반환하도록 했습니다.

### AI 및 보안

- OpenAI 클라이언트를 AI 리포트 요청 시에만 생성하도록 변경했습니다.
- `OPENAI_API_KEY`가 없을 때는 앱을 정상 부팅하고 AI 설정 안내를 반환합니다.
- AI Markdown을 HTML로 변환한 뒤 bleach로 정화합니다.
- AI 리포트와 메일 알림 POST 엔드포인트에 관리자 토큰 및 IP 기반 요청 제한을 추가했습니다.
- DB 및 메일 예외는 서버 로그에만 기록하고 사용자 응답에는 내부 정보를 노출하지 않습니다.

### 데이터 표시

- DB의 `default_prob` 기준을 0~1로 고정하고 화면 및 차트 전달값은 0~100 퍼센트로 통일했습니다.
- 프런트엔드 차트의 중복 퍼센트 변환을 제거했습니다.
- 업종 비교 표현을 실제 `median_*` 계산 방식에 맞춰 중앙값으로 정정했습니다.
- Beneish M-Score가 없을 때 템플릿 숫자 포맷 오류가 나지 않게 했습니다.

### 개발 환경

- `.gitignore`, `.env.example`, pytest 설정 및 회귀 테스트를 추가했습니다.
